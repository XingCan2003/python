#向磁盘文件输入字符串，#停止输入
from sys import stdout
filename = input('请输入文件名:')
fp = open(filename,"w")
ch = input('请输入字符串:')
while ch != '#':
    fp.write(ch)
    stdout.write(ch)
    ch = input('')
fp.close()
